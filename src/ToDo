Lexer:

FirstParse:
    -parseType function allows for multiple of the same type in variant type

SecondParse:

General:
    -Currently, int x += 2; is valid. Make that an error.
    -Make it so functions can return variant types
        -Actually, make it so that variant types even have functionality

    -Add cast functionality
    -Add @ functionality