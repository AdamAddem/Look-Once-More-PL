__C printf(raw char fmt, __va);

pub fn main() i32 {
    $[20]i64 array = junk;

    $u32 n = 0;
    $i64 x = 0;
    x = n;
    while n < 20 {
        # array[n] = n;
        ++n;
    }

    n = 0;
    while n < 20 {
        # __C.printf("%d\n", array[n]);
        ++n;
    }

    return 0;
}