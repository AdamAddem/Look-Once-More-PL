Lexer:
    -Reserve token_handler prematurely using some metric based on the size of the file
    -Refactor token_handler to return views of tokens instead of creating a new token_handler

First/Second Parse:
    -Add do-while and switch

Grammar:
    -Refactor types to return string views
    -Fully integrate the generated operator enum
    -Make left and right associative categories within the generator definition

Validation:

Backend:
    -Various math functions only use the signed versions, change
    -Exponent operator unimplemented

General:
    -Create and revisit promotion rules for arithmetic operations
    -Consider creating rules on limiting sizes of names / parameter lists / etc for the sake of compiler performance
    -Refactor line_number details to be stored in the top level statement and expression objects
        -Validation functions will likely need to take a general Statement* and then make assumptions about type from there
    -Add variant functionality
    -Add cast functionality
    -Add @ functionality //partially complete
    -Add array types